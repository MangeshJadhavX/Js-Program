const str = "     Mangesh     Jadhav    ";

console.log(str.trim());   // its trim only first and last spacing

console.log(str.toUpperCase());   //  upperCase
console.log(str.toLocaleLowerCase())//lowerCase 

