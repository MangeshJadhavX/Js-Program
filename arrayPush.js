const initialArray = [1,2,3,4,5];
initialArray.push(6);  //  adds a element at end

console.log(initialArray);

//////////////////////////////////////////////////////////////////

const initialUnArray = [1,2,3,4,5];
initialUnArray.unshift(0);  //  adds a element at first

console.log(initialUnArray);

//////////////////////////////////////////////////////////////////

const initialPArray = [1,2,3,4,5];
initialPArray.pop();  //  remove the last element

console.log(initialPArray);

////////////////////////////////////////////////////////////////////////

const initialSArray = [1,2,3,4,5];
initialSArray.shift();  //  put first element menace remove first

console.log(initialSArray);